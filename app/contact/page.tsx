import ContactHero from "@/components/hero/ContactHero";
import React from "react";

const page = () => {
	return (
		<main className="contact">
			<ContactHero />
		</main>
	);
};

export default page;
